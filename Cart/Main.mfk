import Game
import Startup
import Title
import random
import BitBusterDepackZ80Ram

void main()
{
	// Set the initial random number seed
	rand_seed	= $0720;

	while (true)
	{
		showTitle();
		startGame();
	}
}
